package ValorRetornoCadena;

public class MetodoCadena5 {
    // Método que obtiene una subcadena de una cadena dada y devuelve el resultado
    public static String obtenerSubcadena(String cadena, int inicio, int fin) {
        return cadena.substring(inicio, fin);
    }
}

