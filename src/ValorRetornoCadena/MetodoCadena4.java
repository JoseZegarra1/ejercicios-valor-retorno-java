package ValorRetornoCadena;

public class MetodoCadena4 {
    // Método que reemplaza todas las ocurrencias de una subcadena en una cadena y devuelve el resultado
    public static String reemplazar(String cadena, String subcadenaAntigua, String subcadenaNueva) {
        return cadena.replace(subcadenaAntigua, subcadenaNueva);
    }
}
